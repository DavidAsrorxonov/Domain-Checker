const Navbar = () => {
  return (
    <div
      className="text-5xl font-bold cursor-pointer"
      onClick={() => window.location.reload()}
    >
      .check
    </div>
  );
};

export default Navbar;
